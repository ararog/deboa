[package]
name = "deboa-macros"
edition = "2021"
version = "0.0.6"
authors = ["Rogerio Araújo <rogerio.araujo@gmail.com>"]
description = "Request macros for the deboa HTTP client alongside bora for quick http client development"
license = "MIT"
repository = "https://github.com/ararog/deboa"
readme = "README.md"
keywords = ["compiler-plugin", "macros"]
exclude = [".travis.yml"]
publish = true
rust-version = "1.64.0"


[features]
default = ["json"]
json = ["deboa-extras/json"]
xml = ["deboa-extras/xml"]
msgpack = ["deboa-extras/msgpack"]

[dependencies]
deboa = { path = "../deboa", version = "0.0.8", features = ["http1"] }
deboa-bora = { path = "../deboa-bora", version = "0.0.5" }
deboa-extras = { path = "../deboa-extras", version = "0.0.4", features = ["json"] }
serde = "1.0.228"

[dev-dependencies]
httpmock = { version = "0.8.2", features = ["http2"] }
tokio = { version = "1.47.2", features = ["rt-multi-thread"] }


