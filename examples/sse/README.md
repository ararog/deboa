# SSE Example

How to use Deboa to make requests to a server-sent events (SSE) endpoint.

This example demonstrates how to handle Server-Sent Events using Deboa's streaming capabilities.

It shows how to set up and consume SSE streams with Deboa's asynchronous HTTP client.

## Running

This example uses JSONPlaceholder API, so it requires an internet connection.

```bash
cargo run
```
