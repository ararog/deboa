# Catcher Example

A simple example showing how to use DeboaCatcher trait to implement a middleware.

This example demonstrates how to create a custom catcher to log requests and handle errors.

## Running

This example uses JSONPlaceholder API, so it requires an internet connection.

```bash
cargo run
```
