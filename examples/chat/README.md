# Chat

A very basic chat application using Deboa and WebSocket from deboa-extras crate.
Application app is built with iced crate.

This example demonstrates how to build a simple chat interface using Deboa's WebSocket support.

## Running

This example uses JSONPlaceholder API, so it requires an internet connection.

```bash
cargo run
```
