# CBOR Example

To try this example you need a server running locally. You can start the provided Python script by running:

```sh
python server.py
```

After the server is up, run the example:

```sh
cargo run -p cbor
```
