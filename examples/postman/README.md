# Postman

A showcase of how to use bora attribute macro from vamo-macro crate to build
a client to make requests to a REST api.

This example demonstrates how to create a simple API client using Deboa's
bora macro for defining endpoints and making requests.

## Running

This example uses JSONPlaceholder API, so it requires an internet connection.

```bash
cargo run
```
